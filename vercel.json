{
  "version": 2,
  "buildCommand": "cd artifacts/ankitos && pnpm run build",
  "outputDirectory": "artifacts/ankitos/dist/public",
  "installCommand": "pnpm install",
  "build": {
    "env": {
      "PORT": "5173",
      "BASE_PATH": "/"
    }
  },
  "routes": [{ "handle": "filesystem" }]
}